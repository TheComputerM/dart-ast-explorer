name: dart_ast_explorer
description: An AST explorer for dart on the web without a backend
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  analyzer: ^6.8.0
  web: ^1.0.0

dev_dependencies:
  dart_style: ^2.3.7
  lints: ^4.0.0
  test: ^1.24.0
  path: ^1.9.0